{
  "id": "openvas",
  "name": "OpenVAS / Greenbone — import rapport GMP",
  "type": "import",
  "category": "vuln-scanner",
  "description": "Importe un rapport XML Greenbone (GMP <report>). Résultats → VULNERABILITY (CVE ou NVT OID) liés à l'asset hôte.",
  "intrusive": false,
  "permission": "connector:openvas",
  "run": "run.py",
  "mapping": "openvas",
  "parameters": [
    { "name": "file", "type": "file", "required": true, "help": "Chemin du rapport XML Greenbone (sur le worker)" },
    { "name": "min_cvss", "type": "string", "default": "0.1", "pattern": "^\\d+(\\.\\d+)?$", "help": "Severité CVSS minimale" }
  ]
}
