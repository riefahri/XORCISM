{
  "id": "agentless-scan",
  "name": "Agentless & offline host scan (credentialed)",
  "type": "import",
  "category": "Vulnerability Scanner",
  "description": "Cyberwatch-style credentialed agentless host scan. Instead of deploying an agent, an admin session (SSH on Linux/Unix, WinRM/PowerShell on Windows) — or an air-gapped collector for segregated networks ('mode déconnecté') — reads the host's OS, installed software and a few security-baseline checks into a JSON snapshot (use the bundled collect.sh / collect.ps1, runnable remotely or locally). This connector ingests that snapshot and maps it to XORCISM: the host becomes an ASSET; the OS and every installed package become CPEs linked to that asset, so the platform's own CVE matcher (cvematch, runs post-import) detects every affected CVE with no active probing — agentless vulnerability detection that then flows into Vulnerability Management, Patch Management, exposure prioritization (EPSS/KEV/SSVC/Exploit-DB) and the attack-path graph. Failed security-baseline checks become hardening VULNs plus a per-host 'hardening level' summary (Cyberwatch-style Compliance Manager); any CVEs already in the snapshot import directly. 100% offline / self-hosted — no host data leaves your infrastructure. Read-only import (no live access, no DB write in run.py — worker-safe). Drives the 'Agentless host scan → vulnerabilities & hardening' attack-chain playbook.",
  "intrusive": false,
  "permission": "connector:agentless-scan",
  "run": "run.py",
  "mapping": "agentless-scan",
  "parameters": [
    { "name": "file", "type": "file", "required": true, "help": "Path on the worker to a host snapshot JSON produced by collect.sh (Linux/Unix) or collect.ps1 (Windows). One host {hostname, os, packages, listening, checks} or many {\"hosts\":[ ... ]}." }
  ]
}
