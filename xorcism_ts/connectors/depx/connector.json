{
  "id": "depx",
  "name": "depx — malicious package / supply-chain audit",
  "type": "tool-runner",
  "category": "sbom",
  "description": "Audits local lockfiles and SBOMs for KNOWN MALICIOUS packages (supply-chain compromise) with ProjectDiscovery depx, cross-referencing the OpenSSF Malicious Packages dataset and a live intelligence feed (hijacked publishes, credential stealers, install-script backdoors, typosquats). Imports the audited project as an ASSET and each malicious-package advisory as a finding (VULNERABILITY / ASSETVULNERABILITY); affected packages are recorded as components/CPEs. Non-intrusive: local read of dependency files + advisory lookups. Requires no secrets/env vars. Install the binary on the worker: curl -sfL https://raw.githubusercontent.com/projectdiscovery/depx/main/scripts/install.sh | sh",
  "binary": "depx",
  "intrusive": false,
  "permission": "connector:depx",
  "parameters": [
    { "name": "source", "type": "string", "required": true, "pattern": "^[A-Za-z0-9 ._/:\\\\-]+$", "help": "Path on the worker to audit: a project directory, a lockfile (package-lock.json, yarn.lock, requirements.txt, go.sum, Gemfile.lock…) or an SBOM (CycloneDX/SPDX)." },
    { "name": "project", "type": "string", "required": false, "pattern": "^[A-Za-z0-9 ._/:@,\\\\-]+$", "help": "Application/asset name to attach the findings to (optional; default « depx audit »)." }
  ],
  "command": ["depx", "audit", "{{source}}", "--json", "--output", "{{outfile}}"],
  "output": { "kind": "file", "ext": "json" },
  "parser": "parse_depx.py",
  "mapping": "depx"
}
