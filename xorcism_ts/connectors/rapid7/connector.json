{
  "id": "rapid7",
  "name": "Rapid7 InsightVM — import assets/vulns",
  "type": "import",
  "category": "vuln-management",
  "description": "Récupère assets et vulnérabilités via l'API InsightVM v3 (console Nexpose) et les importe en VULNERABILITY (CVE). Config : R7_API_URL, R7_API_USER, R7_API_PASSWORD, R7_INSECURE (optionnel).",
  "intrusive": false,
  "permission": "connector:rapid7",
  "run": "run.py",
  "mapping": "rapid7",
  "parameters": [
    { "name": "max_assets", "type": "int", "default": 200, "min": 1, "max": 5000, "help": "Nombre max d'assets à importer" }
  ]
}
