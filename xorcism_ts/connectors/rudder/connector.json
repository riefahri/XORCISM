{
  "id": "rudder",
  "name": "Rudder — nodes & compliance import",
  "type": "import",
  "category": "compliance",
  "description": "Imports managed nodes and configuration-compliance state from a Rudder server (https://www.rudder.io) via its REST API (X-API-Token). Each managed node becomes an ASSET (hostname, IP, OS); installed software (opt-in) becomes a CPE linked to the node; each rule a node is not fully compliant with becomes a finding (ref RUDDER-<ruleId>, severity from the compliance %). Read-only, non-intrusive. Configuration via worker environment variables (never entered in the UI): RUDDER_URL (e.g. https://rudder.example.com — the /rudder/api/latest path is appended automatically), RUDDER_API_TOKEN, RUDDER_VERIFY_TLS (set 0 to skip TLS verification for self-signed servers). Requires `pip install requests` on the worker.",
  "intrusive": false,
  "permission": "connector:rudder",
  "run": "run.py",
  "mapping": "rudder",
  "parameters": [
    { "name": "scope", "type": "enum", "default": "all", "values": ["all", "nodes", "compliance"], "help": "Import inventory only (nodes), compliance findings only, or both (all)." },
    { "name": "limit", "type": "int", "default": 1000, "min": 1, "max": 20000, "help": "Maximum number of nodes to import." },
    { "name": "min_compliance", "type": "int", "default": 100, "min": 0, "max": 100, "help": "Flag a node↔rule as a finding when its compliance %% is below this threshold (default 100 = any rule not fully compliant)." },
    { "name": "include_software", "type": "bool", "default": false, "help": "Also import each node's installed software as CPEs (can be large)." }
  ]
}
